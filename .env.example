# Escada AI uses an OpenAI-compatible chat-completions endpoint.
# Keep these values on the internal server. Never expose them through NEXT_PUBLIC_* variables.
ESCADA_AI_BASE_URL=https://your-ai-gateway.internal/v1
ESCADA_AI_MODEL=your-model-id
ESCADA_AI_API_KEY=replace-with-server-secret
