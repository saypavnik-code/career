#!/usr/bin/env python3
"""Rewrite handoff.md in one shot. Run from /workspaces/career:
    python3 write_handoff.py
"""

content = """# Эскада — session handoff

**Repo:** `saypavnik-code/career` · branch `main` only · dev в GitHub Codespaces `/workspaces/career`
**Last confirmed on origin/main:** `08cf712` — sync commit (handoff.md актуализирован, дубликат SKIP_WAITING listener в scripts/build-pwa-sw.mjs убран после случайного повторного локального применения v28)

## Что сделано (v24-v28, все на main)
- v24: 12 competency `summary` заменены на короткие описательные тексты
- v25: favicon fix, контраст в AI-подсказке Идеи, tooltip по компетенциям
- v26: контраст `<strong>`-заголовков, убран sidebar-блок "Текущий уровень", Отчёты сведены к дате+типу+кнопке, редактор вынесен в `ReportDraftModal`
- v27: экспорт отчёта — кнопки "Скопировать" (`navigator.clipboard`) и "Печать / PDF" (`window.print()`) в `ReportDraftModal`. Без новых зависимостей.
- v28: устранён "зависший" тост обновления PWA. Причина — `sw.js` не вызывал `self.skipWaiting()`. Исправлено: `message`-listener на `SKIP_WAITING` в генераторе SW + тихий одноразовый `window.location.reload()` на `controllerchange`, отложенный, если открыт незасейвленный редактор.

## Подготовлено, но ЕЩЁ НЕ ЗАПУЩЕНО
`escada_patch_v29.py` лежит в корне репозитория, готов к запуску:

    python3 escada_patch_v29.py /workspaces/career

Содержание v29 — личный фокус на цикл (1-3 компетенции): обязательный выбор в онбординге, предзаполнение новых Idea/Win, "Мой путь" в Рост скопирован на фокус, смена фокуса позже через "Изменить фокус". Полностью протестирован (47/47 тестов, реальный next build) на симуляции против чистого post-v28 main перед выдачей — но НЕ ВЫПОЛНЯЛСЯ в Codespaces, поэтому не в этом списке "Что сделано".

Перед запуском v29 — стандартная проверка: git status --porcelain должен быть пуст (кроме самого escada_patch_v29.py), git pull --ff-only origin main актуален. Если git log origin/main расходится с 08cf712 на момент запуска — сначала разобраться в расхождении, не гадая.

## Открытый артефакт
escada-product-roadmap.md в репозитории — полный продуктовый разбор. Нумерация в этом документе НЕ совпадает с реальной нумерацией патчей 1:1 (там v27/v28 обозначают другой порядок пунктов, чем фактически применённые патчи с тем же номером). При сверке всегда ориентироваться на git log origin/main, а не на номера в roadmap-документе.

Оставшиеся пункты по факту (после того как v29 применится):
- v30 (следующий приоритет после v29): "Шкала" как ответ, а не энциклопедия — свернуть 149 критериев за раскрывающийся блок, по умолчанию показывать только релевантное (текущий/следующий уровень, неподтверждённое по фокусу), добавить поиск/фильтр
- v31: мягкие напоминания о ритме (PWA push, без сервера) — раз в 1-2 недели без записей; индикатор "N дней без записи"
- v32+: видимость для руководителя (требует бэкенда — сейчас всё в localStorage, single-player); более лёгкий переход idea→win одним касанием; сравнение "было/стало" как визуальный артефакт отчёта

## Протокол (не менять)
1. Проверить чистый main, git pull --ff-only origin main, сверить git log --oneline с ожидаемым baseline
2. Точечные str.replace с проверкой уникальности (count == 1, иначе REFUSING без изменений)
3. npm ci && npm run test:escada && npm run build
4. commit + push только при полном успехе всех предыдущих шагов
5. Патч — новый файл escada_patch_vN.py (никогда не переиспользовать имя), плюс команда запуска для Codespaces
6. При "не сработало" — сначала git log origin/main, чтобы понять: скрипт не запускали или запускали, но результат не тот. REFUSING при повторном запуске уже применённого патча — ожидаемое поведение, не баг
7. Урок из 25 авг 2026: если git status показывает неожиданные изменения перед запуском нового патча — остановиться и разобраться (diff по каждому файлу), а не запускать патч поверх. В тот раз handoff.md оказался нормальной актуальной версией, а build-pwa-sw.mjs содержал случайно продублированный блок из повторного локального прогона v28 — убрали вручную, перепроверили тестами и билдом, затем закоммитили отдельно от любого патч-скрипта
8. Урок из той же сессии: не использовать bash heredoc (cat > file << 'EOF') для многострочных файлов при вставке через терминал Codespaces — построчная вставка иногда ломает heredoc и остаток контента исполняется как отдельные команды. Вместо этого — короткий Python-скрипт с многострочной строкой, запускаемый одной командой.

## Следующий номер патча
v30 (после того как v29 будет запущен и подтверждён на origin/main)

## Стиль общения
Русский, структурировано, по делу, без воды. Технический код — на английском внутри файлов, UI — на русском. При продуктовых вопросах — сначала анализ реального кода (grep/view), потом рассуждения.
"""

with open("handoff.md", "w", encoding="utf-8") as f:
    f.write(content)

print("handoff.md rewritten successfully.")
