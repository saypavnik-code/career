# Эскада — session handoff

**Repo:** `saypavnik-code/career` · branch `main` only · dev в GitHub Codespaces `/workspaces/career`
**Last confirmed on origin/main:** `3dd9f8f` — v28 (PWA update toast fix)

## Что сделано (v24–v28)
- v24: 12 competency `summary` заменены на короткие описательные тексты (dashboard/ⓘ-friendly)
- v25: favicon fix (app/icon.svg → public/favicon.svg, ручная регистрация с basePath), контраст в AI-подсказке Идеи, короткий tooltip по компетенциям под чипами
- v26: добила контраст (лаймовые `<strong>`-заголовки → ink), убрала sidebar-блок "Текущий уровень", Отчёты сведены к дате+типу+wins/ideas+кнопке, редактор/review/save/история вынесены в `ReportDraftModal`
- v27: закрыта воронка — экспорт отчёта. В `ReportDraftModal` добавлены кнопки "Скопировать" (`navigator.clipboard`, fallback на `execCommand`) и "Печать / PDF" (открывает print-friendly окно с заголовком/периодом/текстом, вызывает `window.print()`). Без новых зависимостей.
- v28 (внеплановый фикс бага, не из roadmap): устранён "зависший" тост "Обновление Эскады готово и применится после перезапуска", появлявшийся при каждой загрузке. Причина — `sw.js` никогда не вызывал `self.skipWaiting()`, новый воркер вечно сидел в `waiting`. Исправлено: `scripts/build-pwa-sw.mjs` теперь генерирует SW с `message`-listener на `SKIP_WAITING`; `CareerDashboard.tsx` сам постит это сообщение и делает одноразовый тихий `window.location.reload()` на `controllerchange`, откладывая его, если открыт незасейвленный редактор (Idea/Win/Report — их состояние живёт только в React state).

## Открытый артефакт
Актуален `escada-product-roadmap.md` — полный продуктовый разбор + roadmap. v27 закрыт. Оставшиеся пункты сдвинуты на один номер вперёд из-за внепланового v28:
- **v29 (следующий приоритет):** личный выбранный фокус компетенций на цикл (не AI-генерация, а осознанный выбор пользователя, 1–3 компетенции). Идеи/Wins по умолчанию предлагают привязку к фокусу.
- v30: "Шкала" как ответ, а не энциклопедия — свернуть 149 критериев за раскрывающийся блок, показывать по умолчанию только релевантное (текущий/следующий уровень, неподтверждённое по фокусу), добавить поиск/фильтр.
- v31: мягкие напоминания о ритме (PWA push, без сервера) — раз в 1–2 недели, если не было записей; спокойный тон, плюс индикатор "N дней без записи" в интерфейсе.
- v32+: видимость для руководителя (требует бэкенда — сейчас всё в localStorage, single-player); более лёгкий переход idea→win одним касанием; сравнение "было/стало" как отдельный визуальный артефакт для отчёта.

## Протокол (не менять)
1. Проверить чистый `main`, `git pull --ff-only origin main`, сверить `git log --oneline` с ожидаемым baseline
2. Точечные `str.replace` с проверкой уникальности (`count == 1`, иначе `REFUSING` без изменений)
3. `npm ci && npm run test:escada && npm run build`
4. commit + push только при полном успехе всех предыдущих шагов
5. Патч — новый файл `escada_patch_vN.py` (никогда не переиспользовать имя), плюс команда запуска для Codespaces
6. При "не сработало" — сначала `git log origin/main`, чтобы понять: скрипт не запускали или запускали, но результат не тот. REFUSING при повторном запуске уже применённого патча — ожидаемое поведение, не баг.

## Следующий номер патча
**v29**

## Стиль общения
Русский, структурировано, по делу, без воды. Технический код — на английском внутри файлов, UI — на русском. При продуктовых вопросах — сначала анализ реального кода (grep/view), потом рассуждения.
