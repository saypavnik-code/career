# Эскада — session handoff

**Repo:** `saypavnik-code/career` · branch `main` only · dev в GitHub Codespaces `/workspaces/career`
**Last confirmed on origin/main:** `cbe03bf` — v26 (heading contrast fix, sidebar "Текущий уровень" removed, Отчёты упрощены до даты + состава + кнопки генерации)

## Что сделано (v24–v26)
- v24: 12 competency `summary` заменены на короткие описательные тексты (dashboard/ⓘ-friendly)
- v25: favicon fix (app/icon.svg → public/favicon.svg, ручная регистрация с basePath), контраст в AI-подсказке Идеи, короткий tooltip по компетенциям под чипами
- v26: добила контраст (лаймовые `<strong>`-заголовки → ink), убрала sidebar-блок "Текущий уровень", Отчёты сведены к дате+типу+wins/ideas+кнопке, редактор/review/save/история вынесены в `ReportDraftModal`

## Открытый артефакт
Отдан `escada-product-roadmap.md` — полный продуктовый разбор + roadmap v27–v31+. Не реализовано, ждёт решения Павла по приоритету. Ключевые пункты:
- **v27 (не начато, высший приоритет по разбору):** воронка обрывается на "оформил" — нет копирования/экспорта готового отчёта наружу (только textarea + Ctrl+C, либо технический JSON-бэкап). Нужна кнопка "Скопировать" + экспорт PDF/Word из `ReportDraftModal`.
- v28: личный выбранный фокус компетенций на цикл (не AI-генерация, а осознанный выбор пользователя)
- v29: свернуть шкалу (149 критериев) — по умолчанию показывать только релевантное, полный список — по клику
- v30: мягкие напоминания о ритме (PWA push, без сервера)
- v31+: видимость для руководителя (требует бэкенда — сейчас всё в localStorage, single-player)

## Протокол (не менять)
1. Проверить чистый `main`
2. Точечные `str.replace` с проверкой уникальности
3. `npm ci && npm run test:escada && npm run build`
4. commit + push только при полном успехе
5. Патч — новый файл `escada_patch_vN.py` (никогда не переиспользовать имя), плюс команда запуска для Codespaces
6. При "не сработало" — сначала `git log origin/main`, чтобы понять: скрипт не запускали или запускали, но результат не тот

## Следующий номер патча
**v27**

## Стиль общения
Русский, структурировано, по делу, без воды. Технический код — на английском внутри файлов, UI — на русском.
